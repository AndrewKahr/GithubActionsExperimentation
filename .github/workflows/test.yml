on: push

jobs:
  Test:
    name: Test
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - run: |
        echo "::error ::Crazy Error"
        echo "::warning ::Crazy Warning"
